# Sessão 01 — Início da implementação

- Data: 2026-07-30
- Objetivo: iniciar implementação do motor de reembolso seguindo T-001/T-002.
- Atividades:
  - Criei a estrutura `src/` e `tests/`.
  - Implementei modelos de entrada (`Colaborador`, `Periodo`, `Despesa`).
  - Implementei parsing JSON de entrada e validação básica.
  - Adicionei teste de parsing usando `exemplos/despesas-exemplo.json`.
  - Confirmei que `python -m pytest` roda e passa os testes iniciais.
- Observações:
  - Ainda não commitei no repositório. Vou manter o fluxo de commits por task.
